# SUMMARY: X post on CompX dongle cross-compatibility

**Claim.** A user reports that Nordic-MCU mice "that run CompX's firmware" pair with each other's 4K dongles (VID 3554, "presumably 3553 is CompX"), with the caveat that the pairing dev software is needed.

**Method.** Search-result title only; the post was not opened.

**Result.** Anecdotal evidence that the CompX 2.4G link and pairing are shared across CompX-firmware devices.

**Evidence tier.** 4 (single social-media claim, unverified).

**Performance.** N/A.

**Correctness.** Unverified. Suggests the RF protocol/pairing is common to CompX firmware, which would make version coupling a firmware-compatibility question rather than a hardware one.

**Relevance to hyperpace.** Weak support that CompX pairing is firmware-defined, so mixing firmware versions across the link is plausible but risky. Flagged as unverified; a hardware experiment would settle it.
