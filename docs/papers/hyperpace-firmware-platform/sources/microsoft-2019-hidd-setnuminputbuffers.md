URL: https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/hidsdi/nf-hidsdi-hidd_setnuminputbuffers
Retrieved: 2026-09-15
Source type: Official API reference (Microsoft Learn, WDK DDI reference). Page metadata: ms.date 2019-06-19, updated_at 2024-02-22.
Retrieval method: WebFetch (page converted to markdown; text below is the returned page text).
Character note: any em-dash or en-dash in quoted text was replaced with an ASCII hyphen; nothing else changed.

# Excerpts

> The **HidD_SetNumInputBuffers** routine sets the maximum number of input reports that the HID class driver ring buffer can hold for a specified top-level collection.

Parameter `NumberBuffers`:
> Specifies the maximum number of buffers that the HID class driver should maintain for the input reports generated by the *HidDeviceObject* collection.

Remarks:
> If **HidD_SetNumInputBuffers** returns **FALSE**, and the Microsoft Win32 **GetLastError** function indicates that an invalid parameter was supplied, the value of *NumberBuffers* is invalid. The HID class driver requires a minimum of two input buffers. On Windows 2000, the maximum number of input buffers that the HID class driver supports is 200, and on Windows XP and later, the maximum number of input buffers that the HID class driver supports is 512. The default number of input buffers is 32.

Related code evidence (retrieved 2026-09-15 from local clones, see sources/libusb-2026-hidapi-c-source.md and sources/ruabmbua-2026-hidapi-rs-crate.md):
- libusb/hidapi `windows/hid.c` line 1029: `if (!HidD_SetNumInputBuffers(device_handle, 64)) {`
- ruabmbua/hidapi-rs `src/windows_native/mod.rs` line 423: `check_boolean(unsafe { HidD_SetNumInputBuffers(handle.as_raw(), 64) })?;`
