URL: https://github.com/tauri-apps/tauri/releases/tag/tauri-v3.0.0-alpha.0
Additional URLs: https://github.com/tauri-apps/tauri/releases/tag/tauri-v3.0.0-alpha.1 ; https://github.com/tauri-apps/tauri/blob/v3/.changes/tauri-tray-ksni.md ; https://github.com/tauri-apps/tauri/blob/v3/crates/tauri/Cargo.toml ; https://github.com/tauri-apps/tauri/blob/v3/crates/tauri-runtime-wry/Cargo.toml ; https://github.com/tauri-apps/tauri/blob/dev/.changes/msrv-1.90.md
Retrieved: 2026-09-15 (release bodies via `gh api repos/tauri-apps/tauri/releases/tags/<tag>`; files from shallow clones of branches `dev` at bca4ca58 and `v3` at f44d1114)
Source type: official release notes and source repository (primary)

Note: a WebFetch summarizer rendered the alpha.0 date as "September 13, 2024"; the GitHub API `published_at` below is authoritative and says 2026.

## Release metadata (gh api)

```
=== tauri-v3.0.0-alpha.0
2026-09-13T01:52:05Z
c4a167b829bac5b452fd5ee3285aec5fa183bfbf
=== tauri-v3.0.0-alpha.1
2026-09-15T10:58:29Z
f44d11142d641534ac0857490df9168550353de7
=== tauri-v2.11.0
2026-04-30T15:57:53Z
=== tauri-v2.10.0
2026-02-02T12:48:27Z
```

Remote branches include `refs/heads/dev`, `refs/heads/next`, `refs/heads/v3`.

## Verbatim excerpts, tauri 3.0.0-alpha.0 release body

> ### What's Changed
> - [`c9277f3c0`] Set MSRV to 1.95.
> - [`19929799f`] First v3 alpha release!

> [`364a0e711`] ([#15882]) `tauri-build` no longer copies the configured resources to the cargo target directory; on desktop, unbundled apps (`tauri dev` / `cargo run`) now resolve resources at runtime from their source paths instead.

> [`fb7bdd95b`] Reduce the size of the resolved ACL embedded in the app by storing the autogenerated command permissions as a `commands` list on the plugin/app manifest instead of two explicit permissions (`allow-$command` and `deny-$command`) per command.

> [`1e5ba7b53`] ([#15985]) The webview runtime is now selected when building the app instead of through Cargo features of the `tauri` crate. Applications depend on the runtime crate directly and pass its attributes to `tauri::Builder::runtime`:
>
> ```rust
> tauri::Builder::default()
>   .runtime(tauri_runtime_wry::Wry::default()) // or `tauri_runtime_cef::Cef::default()`
>   .run(tauri::generate_context!())
>   .expect("error while running tauri application");
> ```
>
> - The `wry` and `cef` features were removed, along with the `x11`, `dbus` and `macos-proxy` features that were forwarded to the wry runtime.

> [`1e5ba7b53`] ([#15985]) Runtime-specific APIs moved from the `tauri` crate to extension traits in the runtime crates, which now depend on `tauri`:
> ...
> - The `tauri::tao` and `tauri::wry` re-exports were removed, use `tauri_runtime_wry::{tao, wry}`.

> [`1e5ba7b53`] ([#15985]) The custom scheme URL format (`tauri://localhost` or `http://tauri.localhost`) is now defined by the runtime instead of the platform

> [`c8c75b1f7`] ([#15787]) The Linux GTK bindings are now selected by the new `gtk3` and `gtk4` features instead of being always on, so `tauri` can be built for either GTK version and compiles on Linux and BSD without any GTK dependency at all.
>
> - Runtime crates enable the right one for you: `tauri-runtime-wry` enables `gtk3` and `tauri-runtime-cef` enables `gtk4`.
> ...
> - Enabling both selects GTK 4, the same precedence `muda` and `tray-icon` use. ... such a binary can only ever run one of them, because GTK 3 and GTK 4 cannot be initialized in the same process.

## Verbatim excerpt, tauri 3.0.0-alpha.1 release body

> ## [3.0.0-alpha.1]
> ### What's Changed
> - [`1fbadc877`] ([#15952]) Internal refactors removing an `Arc` and a `Mutex`.

## Verbatim excerpt, `v3` branch `.changes/tauri-tray-ksni.md` (also in `crates/tauri/CHANGELOG.md` line 76 under commit c8c75b1f7, PR #15787)

> The Linux tray icon now uses the ksni (StatusNotifierItem over D-Bus) backend by default instead of libappindicator, dropping the libayatana-appindicator system dependency. The `tray-icon` feature no longer needs a GTK version to be selected. Enable the new `linux-libappindicator` feature to go back to the libappindicator backend.

## Verbatim lines, `v3` branch Cargo manifests

- `crates/tauri/Cargo.toml` line 188: `tray-icon = ["dep:tray-icon", "tray-icon?/ksni"]`
- `crates/tauri/Cargo.toml` lines 192-193: `gtk3 = ["dep:gtk", "muda/gtk3"]` / `gtk4 = ["dep:gtk4", "muda/gtk4"]`
- `crates/tauri-runtime-wry/Cargo.toml` line 16: `wry = { version = "0.56.0", default-features = false, features = [`
- `crates/tauri-runtime-wry/Cargo.toml` line 45: `webkit2gtk = { version = "=2.0", features = ["v2_40"] }`
- `Cargo.toml` line 49: `rust-version = "1.95"`

## Verbatim, `dev` branch `.changes/msrv-1.90.md` (unreleased, targets the next 2.x minor)

> Set MSRV to 1.90.

## Related verbatim, `dev` branch `crates/tauri/Cargo.toml` feature list (Tauri 2)

```
# TODO: Remove in v3 - wry does not have this feature anymore
objc-exception = []
```

## Related verbatim, updater docs (https://v2.tauri.app/plugin/updater/)

> `createUpdaterArtifacts` | Setting this to `true` tells Tauri's app bundler to create updater artifacts. If you're migrating your app from an older Tauri version, set it to `"v1Compatible"` instead. **This setting will be removed in v3** so make sure to change it to `true` once all your users are migrated to v2.

## Related verbatim, maintainer comment on tray-icon issue #336 (FabianLars, 2026-07-07)

> maybeee we should ignore it since we'll be switching out appindicator for ksni soon anyway.
