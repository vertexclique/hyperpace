# features

Per-feature design docs: the feature shape, the rules, the surfaces it touches.
A feature doc is the conceptual workspace; the implementation contract is its
plan under docs/plans/. Keep the two in sync; the plan is what an implementer
reads verbatim.
