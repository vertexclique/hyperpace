# incidents

One file per incident, dated (YYYY-MM-DD-<slug>.md). Record the symptom, the
runtime evidence, the root cause (the exact code path), the fix, the
verification, and the remaining follow-ups named honestly. The reasoning is the
value: a future reader should learn the lesson the incident bought.
