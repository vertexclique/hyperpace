# architecture

System design and the load-bearing decisions: the component map, the algorithms,
the data model, the scope, the performance budgets. Diagrams are standalone
.mermaid files with a companion .md for prose. Cross-reference these docs from
code by name.
