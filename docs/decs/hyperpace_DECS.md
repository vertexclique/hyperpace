# Decision log: Hyperpace autonomous run

Plan: `docs/plans/hyperpace.md`. Mode: loop, started 2026-09-16 on the operator's instruction
"Run all phases back to back" and "Finish everything". In this mode I do not stop at phase gates;
every decision I take alone is recorded here for review.

Format: one row per decision, newest at the bottom.

| # | Phase | Decision | Why | Alternative not taken | Reversible? |
|---|---|---|---|---|---|
| 1 | 1 | Workspace edition 2024, resolver 3, lints `missing_docs`, `unsafe_code = forbid`, clippy all plus pedantic, `unwrap_used` and `expect_used` warned | Matches the doctrine's no-panic-on-production-path rule and the toolchain present (rustc 1.98) | Edition 2021 to match the store dependency's own edition; not needed, since editions compose per crate | Yes, one manifest |
| 2 | 1 | `Frame::decode` does not enforce the checksum; `Frame::checksum_valid` is separate | A device reply is useful even when its trailing byte is not what a request carries, and the research never proved the device checksums its responses | Reject frames with a bad checksum, which would drop valid replies if the firmware does not checksum | Yes |
| 3 | 1 | `declared_payload()` clamps the device supplied length byte to the real payload size | The length byte is device supplied and is known to understate the payload for at least one command; an unclamped index would panic | Trust the byte, which risks a panic on the device path | Yes |
| 4 | 1 | `Status::Other(u8)` keeps unknown status bytes verbatim instead of mapping them to an error | The firmware's full status set is unknown; discarding the value would lose evidence | Treat anything above 1 as a hard error | Yes |
| 5 | 1 | Test modules allow `clippy::unwrap_used` | The doctrine bans panics on production paths, and explicitly permits assertions in tests | Result-returning tests everywhere, which hides the failing line | Yes |
| 6 | 1 to 8 | The `Transport` trait is defined in `hyperpace-protocol`, not in `hyperpace-device` | It is pure data flow with no I/O, and placing it in the protocol crate lets the firmware crate depend on protocol alone, so device and firmware can be built in parallel | Keep it in the device crate and make firmware depend on device, which serializes the build and couples firmware to hidapi | Yes, a file move |
| 7 | 1 to 8 | I pre-created the four crate skeletons and registered every workspace member myself | Parallel coding agents would otherwise edit the root manifest at the same time and lose each other's edits; the skeletons contain documentation only, no stub logic | Let each agent add its own member entry | Yes |
| 8 | 1 to 8 | Implementation fanned out to coder agents on the fast model, staged protocol and store, then device and firmware, then app and UI, then packaging, then gate rounds | The doctrine reserves the reasoning tier for design and delegates code writing; the staging matches the dependency order so no agent waits on a peer's files | One sequential implementer, which would take far longer for the same result | Yes |
| 9 | 7 | Firmware tests parse the archived sibling images read-only and assert they are classified as foreign | It proves the parser and the identity gate against real vendor containers without any genuine package for this hardware | Synthetic fixtures only, which would not prove the gate rejects real foreign images | Yes |
| 10 | Integration | The Tauri app now points at the real UI build (`../../ui/build`) with a `beforeBuildCommand` that builds it, and the placeholder page under the app crate was deleted | The app shipped by the agents pointed at a placeholder, so it would have launched without the interface; this is a defect fix, not a preference | Keep the placeholder and copy the UI output into it at package time, which duplicates artefacts and drifts | Yes |
| 11 | Integration | Dependency policy extended with MPL-2.0, Unlicense and BSL-1.0 after naming every crate that carries them | The nine rejected crates are all transitive (webview CSS stack, the Linux tray backend, a hashing crate); all three families permit linking and redistribution in a binary, and refusing them would mean dropping the webview or the tray | Vendor or replace those dependencies, which is not possible for the webview stack | Yes |
| 12 | Integration | Two RUSTSEC advisories accepted by id with written reasons: fxhash (RUSTSEC-2025-0057) and proc-macro-error (RUSTSEC-2024-0370) | Both are unmaintained notices with no reported vulnerability and no safe upgrade, and both are baked into third-party manifests with no feature to disable them; they are recorded in `deny.toml` rather than silently suppressed | Leave the gate red, which would make the gate meaningless as a signal | Yes, on the next dependency upgrade |
| 13 | Integration | Makefile `clean` no longer deletes `dist/` | `dist/` is source-controlled packaging input, not build output; the target now removes `target` and the UI build output instead | Rename the packaging directory to avoid the clash, which contradicts the operator's instruction that packaging lives under `dist` | Yes |
| 14 | Integration | The whole frontend to backend IPC layer was realigned to the Rust command shapes, and a guard test now pins command names and payload fixtures | The UI and the app crate were built in parallel against the contract and drifted: `connect`, `save_macro`, `write_setting`, `set_button`, `delete_macro`, `app_settings` and `firmware_install` all had wrong argument names or shapes, and no compiler checks that boundary, so every one of those actions would have failed at runtime | Leave it to be discovered by clicking through the app, which is exactly the false green the doctrine forbids | Yes |
| 15 | Integration | Rejected the agent's removal of the long range and DPI indicator UI; implementing both end to end instead | They are parity features named in the approved plan's feature table. A missing backend field is a gap to close, not a reason to delete the interface; removing them would have been a silent scope cut | Accept the deletion and mark the features deferred, which the operator never agreed to | Yes |
| 16 | Integration | A device that answers status 1 (unsupported) for long range is reported as unsupported in the DTO, never as a silent "off" | Honesty fence: a false "off" reads as device data when it is really an absent capability | Map unsupported to false, matching the vendor app's own error hiding | Yes |
| 20 | Integration | `scripts/gate.sh` now runs the frontend build and type check after the Rust gate | The installed gate covers only the Rust workspace, so a broken or drifted interface passed green; the UI is a shipped surface and belongs in the gate. This is the same class of hole that let the IPC payloads drift unnoticed | Leave the frontend outside the gate and rely on remembering to run npm by hand | Yes |
| 21 | 1 | The differential oracle drives the vendor's own code inside the sandbox rather than a transcription of it, and its fixture records both bundle hashes | A transcription would only test my reading of the vendor code; driving the real functions tests the code that actually works with the device, and the hashes tie the fixture to the exact bundles committed in the archive | Hand-written vectors only, which is what the tree had and which cannot catch a misreading | Yes |
| 17 | Phase 4 (long range and DPI indicator) | The DPI indicator's four sub-fields (mode, brightness, speed, on) each got their own `WriteSettingRequest` variant instead of one combined struct like `Lighting` | They are four independent scalar addresses (76/78/80/82), not one packed wire struct the way `Lighting`'s 7-byte record is; every other independent scalar in the enum (Debounce, MotionSync, PerformanceOn/Timeout, Sleep, ...) already gets its own variant, so this matches the dominant shape, not the one Lighting-shaped exception | One `DpiIndicator { value: DpiIndicatorDto }` variant writing all four scalars together, mirroring `Lighting` | Yes |
| 18 | Phase 4 (long range and DPI indicator) | `SettingsDto::from(Settings)` fills `long_range` with a documented placeholder (`Unsupported`) that `commands::device::read_settings` overwrites immediately via a dedicated `GetLongRangeMode` request | Long range has no flash address, so nothing in `Settings` (decoded from the shadow alone) can supply it; every other `SettingsDto` field is a straight 1:1 mirror, so this is the one field that needs a second source, and the placeholder is never observed outside that one call site | A second constructor (`SettingsDto::from_parts`) instead of reusing `From<Settings>` plus an overwrite, which would touch every existing call site of the blanket conversion for one field | Yes |
| 19 | Phase 4 (long range and DPI indicator) | UI placement: long range is a toggle on the Performance screen (matching the vendor OLD driver's own placement, section 10.4); the DPI indicator is its own panel on the Lighting screen, sharing that screen's existing `draft`/`apply()` state with the mouse body lighting panel, with its own "Apply changes" button | Long range is a power/range setting, not a light; the DPI indicator is genuinely part of `Settings`/`diffSettings` like mouse lighting is, so reusing the same draft avoids a second parallel settings-editing state machine on one screen | A dedicated "DPI indicator" screen, which the plan's screen list (`docs/architecture/api-contract.md`'s UI section: Buttons, Performance, Macros, Lighting, Firmware, Settings) does not name | Yes |
| 22 | Phase 7 (firmware, routes 1 and 2) | The two "configuration files the verdict names" for route 1 are `www.lofree.tech/cfg.json` (root, cid 102, zero `upgrade` keys) and `www.lofree.tech/home/cfg.json` (chooser template, cid 62, dead Pulsar `upgrade` block) | These are the only two cfg.json files with a sha256 both recorded in the research AND archived locally as a fixture (`firmware/archive/.../_web-driver-snapshots/.../www.lofree.tech/{cfg.json,home/cfg.json}`), so their hashes are independently verified by a test against real bytes rather than copied from prose alone | `www.lofree.tech/mouse/cfg.json` (the production driver) and `/keyboard/cfg.json`, named together in the verdict's own bottom-line paragraph, but neither has a sha256 recorded anywhere nor a local snapshot to test against | Yes, swap the constants |
| 23 | Phase 7 (firmware, route 1) | The six firmware directory probe targets are `/mouse/bin/`, `/home/bin/cid102_mid0{1,2}/` and `/keyboard/bin/cid102_mid0{1,2}/` and `/keyboard/bin/cid62_mid01/` | These are exactly the cid 102 (this mouse's own) and cid 62 (the chooser's) entries among the paths `FIRMWARE-VERDICT.md` candidates 22 to 24 name | Also probing the verdict's cid 66 / cid 87 / `/OE921/` rows, which belong to unrelated products with no established tie to this hardware | Yes, extend the list |
| 24 | Phase 7 (firmware, route 1) | `hyperpace-app`'s HTTP client uses `reqwest`'s `rustls-no-provider` feature plus a direct `rustls = { features = ["ring", ...] }` dependency and one `install_default()` call at startup, instead of `reqwest`'s own default `rustls` feature | `reqwest`'s default TLS feature (and `rustls`'s own default feature set) unconditionally pulls in `aws-lc-rs`/`aws-lc-sys`, a native C/cmake build step nothing else in this workspace needs; `rustls-no-provider` plus an explicit `ring` install adds zero new crates to `Cargo.lock` (confirmed by inspection: the minimal feature set added only the `hyperpace-app -> reqwest`/`rustls` edges, no new `[[package]]` entries) | `reqwest`'s default features, which was tried first and reverted after it added `aws-lc-rs`, `aws-lc-sys`, `cmake`, `fs_extra`, `jobserver` and `pkg-config` to the lock file | Yes |
| 25 | Phase 7 (firmware, route 1) | `firmware_watch_enabled` app setting defaults to enabled (absent means on), and the background watcher's periodic interval is 24 hours | The plan ships all three acquisition routes enabled by default; "at most once per app start plus a slow periodic check" needed a concrete slow interval, and a day is slow enough that it can never be mistaken for a busy loop while still catching a vendor publication within about a day | A shorter interval (hourly), which is not "slow" by the plan's own wording, or defaulting the setting off, which would silently disable a route the operator chose to ship | Yes, both are one constant |
| 26 | Tooling | Corrected my own mistake: a `git checkout -- Cargo.lock crates/hyperpace-app/Cargo.toml docs/plans/hyperpace.md` run to revert a rejected `reqwest` feature experiment also discarded a pre-existing, unrelated uncommitted edit to `docs/plans/hyperpace.md` (the "Known gaps still open" table) that I had not touched and had not checked `git status` for first | The instructed safety step (check `git status` and stash before a destructive git command) was skipped; the exact diff had been shown in an earlier tool result in this same session, so it was retyped back verbatim and reconciled with `git diff` to confirm a byte-for-byte match, rather than left lost | Leaving it discarded and not mentioning it | No further action needed; already restored and verified identical |

| 27 | Known gaps (keystroke editing) | Kept `set_button`'s existing `keystroke` field as the one write path for a button's chord; only added a read command (`get_button_keystroke`), not a parallel `set_keystroke` | `set_button` already wrote the keystroke slot alongside the button record whenever a caller supplied one; adding a second write command for the same slot would be two places writing the same bytes, the exact divergence risk the doctrine singles out | A dedicated `set_keystroke(index, keystroke)` command mirroring `get_button_keystroke`, which would duplicate `apply_set_button`'s write for no behavioral gain | Yes |
| 28 | Known gaps (keystroke editing) | Added `DeviceHandle::read_block` (chunked `ReadFlashData`, mirroring `write_block`) rather than widening the connect walk | The keystroke region starts at offset 256, past the walk's fixed 256-byte span (`docs/research/mouse-protocol-v2.md` 6.1); widening the walk to 16 KiB would read the whole shadow, including the macro region, on every connect for a feature that touches one 32-byte slot at a time | Read every button's keystroke slot during connect, which reads far more than any screen needs to show, unconditionally, on every connection | Yes |
| 29 | Known gaps (keystroke editing) | `get_button_keystroke` refuses `index >= table.buttons` before touching the device | The address formula (`offset::KEYSTROKE + 32 * index`) accepts any `u8` and would silently read a neighbouring button's slot (or the macro region, once index is large enough) for an out-of-range index; this is the trust-boundary validation the doctrine requires for untrusted input from the UI | Leave it unchecked, matching `apply_set_button`'s own button-record write, which has the same latent gap but was not part of this closure | Yes |
| 30 | Known gaps (pairing and profile) | `ProfileDto` is a tagged `{Active{index}, Unsupported}` enum, not `Option<u8>` | `None` cannot distinguish "not read yet" from "this device has no profile switching"; `GetCurrentConfig`'s own status-1 case (section 10.2) needs the same honest three-state handling `LongRangeDto` already models for `GetLongRangeMode` | `Option<u8>`, which is shorter but reintroduces the exact ambiguity the honesty fence forbids | Yes |
| 31 | Known gaps (pairing and profile) | Pairing progress streams through a `Channel<PairStateDto>` on `pair_receiver` (mirroring `firmware_install`'s existing progress channel), polled with a real 1 s cadence up to 20 ticks (section 10.1); profile changes are instead picked up by the already-existing `StatusChanged.profile` push -> `refreshSettings()` path on the UI side, with no new polling added for it | The protocol only pushes for a profile change (`StatusChanged` bit 0x04); pairing has no push at all and is inherently polled by the device's own design, so "prefer a device event over polling" applies to profile (reuse the existing push-driven refetch) and not to pairing (there is no push to prefer) | A generic `DeviceEvent::PairState` pushed from the device layer, which would fabricate a push the protocol does not have, or polling profile client-side on a timer, which the existing `Changed` event already makes unnecessary | Yes |
| 32 | Known gaps (structured logging) | `tracing-subscriber`'s `EnvFilter` feature is used (defaults to `info`, overridable via `RUST_LOG`) rather than a fixed compiled-in level | Every dependency `env-filter` needs (`matchers`, `regex-automata`) was already fully resolved in `Cargo.lock` before this change (confirmed by inspection: `loom`, an existing dev-dependency elsewhere in the graph, already enables it), so this costs nothing extra in binary size or build time over a fixed level, while being far more useful for diagnosing a report from the operator | A hardcoded `.with_max_level(Level::INFO)`, which would need a rebuild to ever see a debug/trace log from a real run | Yes |
| 33 | Known gaps (simulator fidelity) | The owner thread now discards its cached `Shadow` and restarts the base settings walk after a `FactoryReset` request the device actually decoded (not status-1) | Modeling factory reset in the simulator (gap 4) surfaced a real staleness bug: `read_settings` only ever returns the owner thread's cached shadow, which nothing invalidated on reset, so a caller doing exactly what the app's own `factory_reset` command does (send the reset, then re-read settings) would keep seeing pre-reset values indefinitely. This mirrors the protocol's own documented behavior (section 10.3: "re-read flash") | Leave the cached shadow untouched and require every caller to somehow know to re-fetch via a raw `ReadFlashData`, which the existing `read_settings` API gives no way to trigger | Yes |
| 34 | Known gaps (simulator fidelity) | "the unsupported-command behaviour for a model that lacks a feature" was implemented as a fix to `factory_reset`, `receiver_light` and pairing's `EnterPair` step: each now checks the reply's status byte (`require_supported`) instead of discarding it | All three previously called `handle.request(...)` and returned `Ok(())`/proceeded regardless of the reply's status, so a status-1 (unsupported) reply from a model that genuinely lacks the feature read back as a false success; the simulator's existing `SimController::set_unsupported` already generically models "a model that lacks a feature" for any command, so the fix is on the app side that must honor it, not a new simulator mechanism | A new simulator-only concept of a "model variant" with features baked in, when the generic per-command unsupported override already covers every case a test needs | Yes |

| 35 | Phase 8 (packaging) | Captured the documented AppImage library-pruning fix as `scripts/package-appimage.sh` (`make package-appimage`), with the exact nine libraries confirmed by diffing an unpruned AppDir's `usr/lib` against a pruned one, plus a bounded retry (max 3 attempts, never unbounded) around `cargo tauri build`'s own appimage step | That step failed outright on 4 of 5 attempts made while doing this work, always the same terse `failed to run linuxdeploy` with no further detail, always after deb/rpm had already bundled; a one-shot manual sequence would not survive being run again, and the doctrine bounds retries with a max rather than looping forever | Leave the fix as the manual sequence this pass ran by hand, which the task explicitly asked not to do | Yes |
| 36 | Phase 8 (packaging) | Recorded, in `docs/plans/hyperpace.md` and `dist/README.md`, that the documented ten-library AppImage prune does not by itself fix this machine's blank-window failure | Fresh testing this pass (the unpruned build, the pruned build, both with and without `WEBKIT_DISABLE_DMABUF_RENDERER=1`) showed the prune alone still fails with `Failed to create GBM buffer...`; this machine's NVIDIA proprietary driver hits a different bug than the AMD/Intel Mesa one the two research summaries document, and claiming the documented fix "worked" here without that fresh evidence would be exactly the false-green the honesty fence forbids | Report only that the prune was applied (true) and let the reader assume it was sufficient (not tested, and turned out not to be, on this hardware) | No, it is what was actually observed |
| 37 | Phase 8 (packaging) | Rebuilt every Linux package (deb, rpm, AppImage, the Arch package) from the current working tree instead of documenting the packages already sitting in `target/`/`dist/arch/` before this pass started | Those pre-existing packages were built from source that predated `crates/hyperpace-app/src/gpu_workaround.rs` (confirmed by comparing file mtimes: the deb/rpm/AppImage on disk were built at 14:49 to 14:52, the fix files at 15:12 to 15:17), so they did not actually carry the NVIDIA fix; shipping/documenting them as final would have been documenting a defect as fixed | Treat the already-built packages as this pass's output and only note the fix exists in source, leaving the shipped bytes stale | Yes, a rebuild |
| 38 | Phase 8 (packaging) | Ran a real `makepkg -f --nodeps` build for the Arch package (not just `--printsrcinfo`, which is all the tree had verified before) | Network was available this pass (unlike whatever constraint produced the earlier "not build-verified, needs network" note), and the only actual blocker was pacman's own dependency check refusing to see `cargo`/`npm` as present since they are rustup/nvm managed on this machine, not pacman packages; `--nodeps` skips only that check, installs nothing and needs no root, so it stays inside the hard rules | Leave the Arch package unverified beyond `--printsrcinfo` a second time, when nothing actually prevented a real build | Yes |
| 39 | Phase 8 (packaging) | Left the deb's duplicate `Depends` entries (`libwebkit2gtk-4.1-0`/`libgtk-3-0` each appear twice: once auto-detected by `tauri-bundler` via `ldd`, once from `tauri.conf.json`'s own manually configured `deb.depends`) as a documented, deferred finding rather than editing `tauri.conf.json` | `dist/README.md` already describes that file as owned by the app crate, not by packaging work, and this task's explicit scope is documentation, scripts and the gate, not a dependency-list edit; the duplicate is functionally harmless (`dpkg` tolerates a repeated `Depends`) | Remove the redundant `deb.depends`/`rpm.depends` arrays from `tauri.conf.json` myself, which is a one-line fix but touches a file outside this pass's ownership without being asked | Yes, whoever owns that file can drop two arrays |
| 40 | Phase 8 (packaging) | Added `dist/arch`'s own `makepkg` build byproducts (`pkg/`, `src/`, `*.pkg.tar.*`, `*.log`) to `.gitignore` | A real build now leaves a fresh multi-megabyte package file and two build-scratch directories inside a source-controlled packaging directory every time it runs; `/target` already gets the same treatment for the same reason, and the alternative is a `git status` that looks dirty after every legitimate build | Leave them unignored and rely on remembering never to `git add -A` in `dist/arch/` | Yes |
| 41 | Phase 8 (packaging) | Launched the built AppImage directly on the operator's live desktop (both the unpatched and the patched build, each bounded by `timeout` and killed afterward) and captured screenshots as the proof a window actually renders, rather than trusting a clean startup log alone | A log with no GBM error is consistent with "launched fine" but does not itself prove a window painted; a screenshot does. Read `crates/hyperpace-app/src/state.rs` first to confirm `connect`'s default backend is the simulator (`real_device` must be explicitly requested), so launching the app this way never touches the real mouse, `/dev/hidraw*` or any USB device | Trust the absence of an error in the log as sufficient proof, which is a weaker claim than what "confirmed by a screenshot" lets the docs honestly say | No, already done; the screenshots are the evidence |

| 42 | Defect fix (blank window, NVIDIA/Wayland) | `gpu_workaround::apply` changes the process's environment by re-executing itself (`std::process::Command::env` plus `CommandExt::exec`, both safe) instead of `std::env::set_var`, and the `gpu_dmabuf_workaround` app-setting override is read through its own store handle, opened and dropped before that re-exec, never the `Store` `run` opens for the rest of the app | This workspace forbids `unsafe_code`, and `set_var`/`remove_var` are `unsafe fn` as of Rust 1.82 (a data race risk on some platforms' libc), so there is no safe in-process way to change this process's own environment after start; re-exec is the safe equivalent. The embedded store holds an exclusive lock on its root for as long as a `Store` handle to it is alive, so carrying one across a re-exec would deadlock the new process's own `Store::open` | Call `env::set_var` inside an `unsafe` block, which `unsafe_code = "forbid"` refuses even with a local `#[allow]`; or reuse `run`'s own store handle for the setting peek, which risks exactly that lock deadlock | Yes |
| 43 | Defect fix (blank window, NVIDIA/Wayland) | Two escape hatches, both on/off/auto, env var taking precedence: `HYPERPACE_DMABUF_WORKAROUND` and a `gpu_dmabuf_workaround` app setting (read/written through the existing generic `app_settings` command). No new Settings-screen control added for the app-setting one | The task scoped this defect to `crates/hyperpace-app`; the environment variable is the documented, user-facing override (README), and the app setting is a forward-compatible hook reachable today through the existing command, not a half-built feature | Add a dedicated Settings-screen toggle for it, which is UI scope this defect never asked for | Yes, a small addition later |
| 44 | Defect fix (window fit) | Default window size 1180x940, minimum 900x620, both measured against the real rendered app, not guessed: 940 is the shortest default height at which the Lighting and receiver screen's three panels, receiver pairing and factory reset included, render with no scrollbar; 900 is where the sidebar-collapse and two-column-stack breakpoints below already engage | Confirmed by screenshotting the actual window on the operator's own machine (a KWin script activates, raises and reports the window's precise geometry, since `spectacle`'s active-window capture and a blind full-desktop crop both gave unreliable results at first on this multi-monitor, fractionally-scaled desktop) at both sizes, not by estimating from CSS alone | A much larger default "to be safe", which the task explicitly said not to do | Yes, one constant each |
| 45 | Defect fix (window fit) | One shared breakpoint, `max-width: 900px`, drives both the sidebar's icon-only collapse (`Sidebar.svelte`) and the Buttons/Macros screens' column-to-stack change, matching the window's own minimum width | Keeps the "minimum width the layout supports" fact in one place; a per-component breakpoint tuned separately for the sidebar vs. each two-column screen is exactly the two-copies-that-can-drift the doctrine warns about | A separate, independently tuned breakpoint per component | Yes |
| 46 | Defect fix (no-device placeholders) | Every screen that edits `Settings` now renders from `BLANK_SETTINGS` (`ui/src/lib/types.ts`, a structurally complete `Settings` whose values are never displayed) instead of conditionally mounting its controls on `device.connected`/`device.settingsLoading`; `RangeField`, `SelectField` and `ColorSwatchPicker` each gained an `unknown` prop that shows a plain hyphen instead of the field's real value or options and forces the control disabled | Keeps "no device yet" as one flag (`hasData`) per screen instead of a null-check on every field, and keeps the honesty rule (never a plausible-looking number) inside the three shared components rather than repeated per call site | A `Settings | null` draft with a null-check wrapped around every field, which is what every screen already had and is exactly the per-field repetition the doctrine flags | Yes |
| 47 | Defect fix (no-device placeholders) | Left `MacrosScreen`'s own "No macro selected" panel and `FirmwareScreen`'s own "No firmware available for this device yet" panel as they were, not folded into the `BLANK_SETTINGS`/`hasData` pattern | Neither is gated on device connection: macros are a local-store feature independent of any device, and the firmware archive's emptiness is a true statement about the local archive (not a "no device" or "loading" placeholder standing in for real data) | Force a default macro selection or a placeholder firmware row to match the other screens, which would misrepresent both as device-gated features when they are not | Yes, if asked |
| 48 | Defect fix (no-device placeholders) | Kept `ButtonsScreen`'s button-list default view (`selected === null`); only removed the "no device"/"reading settings" `EmptyState` branches around it and the editor pane, and `loadKeystroke` now skips the `get_button_keystroke` round trip entirely (not call-then-catch) when no device is connected | The list is a real interaction pattern (pick a button, then edit it), already matching "as it appears when a device is connected"; skipping a call that can only fail with "no device connected" is more honest than calling it and swallowing that error | Auto-select the first button so the editor always shows by default, which changes existing navigation behaviour beyond what this defect asked for | Yes |
| 49 | Process | Found this working tree already carrying uncommitted changes from a concurrent session on this same repository (entries 35 to 41 above, plus `Makefile`, `.gitignore`, `dist/*`, `crates/hyperpace-app/tauri.conf.json`, `docs/plans/hyperpace.md`, `scripts/package-appimage.sh`, `docs/architecture/ui-controls.md`) while working these three defects; left every one of those files untouched and scoped every edit above to the files the task named | Touching those files risked losing or conflicting with another session's in-progress work that is not mine to resolve | Reset the tree to a "clean" starting point before working, which would have discarded that other work | N/A, nothing of mine to reverse |

| 50 | Real device | The app connects to the operator's own mouse by itself: once at startup and again whenever the hotplug watcher reports a device arriving, asking for read-write access, in a new `autoconnect` module | The operator authorized real-device use on 2026-09-16 ("Now you can switch to real mouse API but you can't break anything"). A configurator whose window shows nothing until someone walks to Settings and picks a backend shows nothing, and read-only access would leave every control in the interface disabled, which the operator has already rejected once ("Nothing must be blocked"). Connecting itself only reads: the handshake and the settings walk. The watcher never touches an existing connection, so an explicitly chosen simulator session is never replaced | Leave connecting a manual step in Settings, or auto-connect read-only and disable every control | Yes, one module and one `setup` line |
| 51 | Real device | On a hotplug removal the watcher drops the connection and then immediately tries once more, rather than only dropping it | The watcher reports a change in the number of attached vendor collections, not which one went away, so a removal that was not this device would otherwise leave the app disconnected with no event left to bring it back. The retry costs one failed open when the mouse really is gone, and reconnects when it is not | Trust the removal to mean this device, and wait for the next arrival event that may never come | Yes |
| 52 | Real device | The tracker clears the connection when its device reports `Disconnected`, before announcing that event | `snapshot()` kept reporting `connected: true` after the owner thread had already given up on the transport, so the tray and any window that asked for the current state were told the device was still there | Leave the stale connection in place until someone calls `disconnect` | Yes |
| 53 | Real device | Every fan-out of a device event to the windows and the tray now goes through one `AppState::announce`; the tracker thread and the hotplug watcher both call it | The tracker had the only copy of that loop, and the watcher needed the same behaviour for the one transition a connected device cannot report, its own unplug. Two copies of "tell everyone" is the divergence the doctrine forbids | Copy the loop into the watcher | Yes |
| 54 | Real device | Tray text is derived from one `TrayStatus` value (presence, link, battery), with the tooltip moved out of `icon.rs` into `tray.rs` | The tooltip read "Hyperpace: not connected" for any state with no battery percent, so a connected mouse whose first battery poll had not landed yet was reported as absent. `icon.rs` owns pixels; the words belong with the rest of the tray's text | Patch the tooltip in place and leave the words split across two modules | Yes |
| 55 | UI | The settings read is now re-entrancy safe (a read requested while one is in flight marks that another is owed instead of starting a second), and a `connected` event triggers one | With the app connecting on its own, no `connect()` call exists to pull the settings in afterwards, and the `connected` event plus a user action could otherwise start two reads of the same shadow at once | Let the event start a second concurrent read, or skip the refresh and show an empty screen after an automatic connection | Yes |
| 56 | Design | Zero radius everywhere; panels, buttons and chips are cut plates (a 45 degree corner cut via `clip-path`), and the manufacturer's own device renders and dial faces ship in `ui/static/device/` | Both are direct operator instructions on 2026-09-16: "I don't want anything to be round in the UI. Make something unique" and "use Lofree's same assets. It is not a big deal to use chinese assets. Use them in the app." The dial faces are kept circular because they are photographs of the hardware; everything around them follows the cut plate. The two faces with the vendor's orange baked into their pixels are not used, so the live value always carries this app's accent | Keep the original SVG line art and rounded cards | Yes, `design.md` records both rules |

| 57 | Real device | `DeviceEvent::Connected` is broadcast when the base settings walk completes, not at the handshake, and a settings read that arrives before any walk has finished waits for it | Verified against the operator's mouse: the UI read settings on `Connected`, got the erased shadow, and failed with "invalid value in field polling rate". The event's own documentation already promised the walk had completed; the code broke that promise | Keep the early event and make every caller poll for readiness | Yes |
| 58 | Real device | Commands that need the mouse wait up to 5 s for it while it sleeps or re-links, instead of failing; the receiver's own commands (online probe, pairing, its light, its version) never wait | The operator's mouse sleeps after 10 s, and a polling rate change re-links it, so requests failed constantly with "no reply before the deadline". Pairing must never wait: a mouse being paired has never answered the receiver. Which commands the receiver answers is one function in the protocol crate, used by both the owner thread and the simulator | Fail fast and ask the operator to retry; or wait on every command, which would deadlock pairing | Yes |
| 59 | Real device | A timeout from the mouse while the owner believes it is ready sends it straight back to the handshake and retries the command within the same 5 s budget | Same cause as 58, seen between two battery polls; re-sending a scalar write is idempotent, so a lost acknowledgement is safe to retry | Wait for the next battery poll to notice, which leaves up to 5 s of failing requests | Yes |
| 60 | Real device | A `StatusChanged` push touching a setting marks the cached settings stale, and the next settings read re-reads the base block first; battery-only pushes do not | Operator: "everytime i change something on the mouse it must be also changed in the ui". The UI already re-read on a push, but the device layer answered from the copy made at connect | Re-walk on every push, including the battery pushes the real mouse sends constantly | Yes |
| 61 | Real device | The connection prefers the mouse's cable over the receiver, falls back to the receiver when the cable cannot be opened, and switches when the cable is plugged in | Operator: plugging the cable in showed "asleep" with no charging state. The kernel log confirmed the cable device enumerates as `3554:fb14` while the receiver loses its link | Stay on whichever connection opened first | Yes |
| 62 | Real device | The pairing request carries the mouse model's cid at payload byte 2 with declared length 2, per the protocol reference | The app sent a bare command-5 frame, which the receiver refused with status 1, reported to the operator as "the device does not support command 5". The request was malformed, not the receiver unsupported | None; the reference is explicit | Yes |
| 63 | Simulator | The simulator answers nothing but the receiver's own commands while the mouse is offline, and its flash can be seeded with real bytes | It answered everything while "offline" and started fully erased, which is why none of 57 to 60 was caught before real hardware. The seeded values in the new tests are the exact bytes read off the operator's mouse | Leave the simulator permissive | Yes |
| 64 | UI | Screens copy device state with `$state.snapshot`, never `structuredClone` | `structuredClone` throws on a Svelte 5 reactive proxy; every screen's copy failed silently and showed the blank defaults as though they were readings, which is what the operator saw as "not showing the current status of the mouse everywhere" | None | Yes |
| 65 | App | Closing the window keeps the app running in the tray; only the tray's Quit exits | The close handler already destroyed the window, but the runtime then exited because nothing refused the resulting exit request. The operator asked for background running with the tray visible | Keep closing as quit, with a preference | Yes |
| 66 | App | The tray icon is the logo itself, with the mouse shell filled to the battery level as a gauge and the percentage inside it | Operator: the tray icon was "not in the style of the hyperpace logo (that holds the battery level)". The shell is enlarged by a fifth inside the frame so two digits fit at tray size | A status-coloured square carrying the logo's corner brackets, which the operator rejected | Yes |
| 67 | App | On Linux, a binary no package installed writes a per-user desktop entry and icon at startup, and never shadows an existing entry | The compositor matches the window's app id to a desktop entry for its icon, so a directly run binary showed a generic placeholder | Leave it to packaging only | Yes |

## Standing constraints during this run

- The app may read from and write settings to the operator's own mouse (authorized 2026-09-16).
  Firmware flashing to it remains unauthorized, and no write has been exercised against it here:
  every test in this workspace still runs against the simulator.
- No claim of hardware verification for anything, firmware flashing above all.
- Firmware install and rollback are implemented but have no genuine package to act on.
- No vendor branding in any product surface; vendor names appear only in `docs/research` and
  `docs/papers` as citations.
- No em-dash or en-dash anywhere.
